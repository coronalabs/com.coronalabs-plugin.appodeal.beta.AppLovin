Sources for the plugin `plugin.appodeal.beta.AppLovin`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.AppLovin" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
